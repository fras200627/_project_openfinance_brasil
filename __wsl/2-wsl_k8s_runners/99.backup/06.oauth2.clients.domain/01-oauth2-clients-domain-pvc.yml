apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: oauth2-clients-domain-pvc
  labels:
    app: oauth2-clients-domain-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
