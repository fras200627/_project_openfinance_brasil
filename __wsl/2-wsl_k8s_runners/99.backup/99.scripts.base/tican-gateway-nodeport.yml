# ---------------------------------------------------------------
# ---------------------------------------------------------------
apiVersion: v1
kind: Service
metadata:
  name: tican-gateway-nodeport
  labels:
    app: tican-gateway-nodeport
spec:
  type: NodePort
  ports:
    - port: 5000
      nodePort: 31001
  selector:
    app: tican-gateway-pod
