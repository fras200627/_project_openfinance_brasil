# ---------------------------------------------------------------
# ---------------------------------------------------------------
apiVersion: v1
kind: Service
metadata:
  name: db-oracle-svc
spec:
  type: ClusterIP
  ports:
    - port: 1521
      targetPort: 1521
  selector:
    app: db-oracle-pod
    