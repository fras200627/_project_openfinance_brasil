apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: products-domain-pvc
  labels:
    app: products-domain-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
