apiVersion: v1
kind: Service
metadata:
  name: discovery-services-svc
  labels:
    app: discovery-services-svc
spec:
  type: ClusterIP
  selector:
    app: discovery-services-pod
  ports:
    - port: 6000
      targetPort: 6000
      
    
  