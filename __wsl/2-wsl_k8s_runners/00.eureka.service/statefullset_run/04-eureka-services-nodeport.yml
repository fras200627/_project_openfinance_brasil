apiVersion: v1
kind: Service
metadata:
  name: discovery-services-nodeport
  labels:
    app: discovery-services-nodeport
spec:
  type: NodePort
  ports:
    - port: 6000
      nodePort: 31000
  selector:
    app: discovery-services-pod
