apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tican-oauth2-users-domain-pvc
  labels:
    app: tican-oauth2-users-domain-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
