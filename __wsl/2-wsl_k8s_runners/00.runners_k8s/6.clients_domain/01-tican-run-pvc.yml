apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tican-oauth2-clients-domain-pvc
  labels:
    app: tican-oauth2-clients-domain-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi

