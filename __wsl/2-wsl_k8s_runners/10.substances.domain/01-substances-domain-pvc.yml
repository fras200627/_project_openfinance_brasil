apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: substances-domain-pvc
  labels:
    app: substances-domain-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
