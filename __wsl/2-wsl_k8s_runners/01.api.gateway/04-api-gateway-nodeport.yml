# ---------------------------------------------------------------
# ---------------------------------------------------------------
apiVersion: v1
kind: Service
metadata:
  name: api-gateway-nodeport
  labels:
    app: api-gateway-nodeport
spec:
  type: NodePort
  ports:
    - port: 6001
      nodePort: 31001
  selector:
    app: api-gateway-pod
