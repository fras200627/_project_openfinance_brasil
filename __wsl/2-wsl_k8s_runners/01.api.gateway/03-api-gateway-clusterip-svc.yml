apiVersion: v1
kind: Service
metadata:
  name: api-gateway-svc
  labels:
    app: api-gateway-svc
spec:
  type: ClusterIP
  selector:
    app: api-gateway-pod
  ports:
    - port: 6001
      targetPort: 6001
