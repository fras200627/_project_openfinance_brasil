apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: api-gateway-pvc
  labels:
    app: api-gateway-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
