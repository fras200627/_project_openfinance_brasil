docker image tag gvenzl/oracle-xe:latest fras200627/openapi/db-tican-oracle:1.0.0
docker image tag mysql:8.0 fras200627/openapi/db-tican-mysql:1.0.0
docker push fras200627/openapi/db-tican-mysql:1.0.0


very good ###################################################
docker-compose kill
docker-compose stop
docker-compose create
docker-compose start

others ######################################################
docker-compose up -d
----------------------------------
docker-compose pause
docker-compose unpause
----------------------------------
<docker-compose down> 
<docker system prune -a -f>
docker volume prune -a -f
docker network prune

#############################################################
docker build -t fras200627/openapi/tican-service-discovery:latest .
docker push fras200627/openapi/tican-service-discovery:latest

docker build -t fras200627/openapi/tican-api-gateway:latest .
docker push fras200627/openapi/tican-api-gateway:latest

docker build -t fras200627/openapi/tican-authentication-server:latest .
docker push fras200627/openapi/tican-service-discovery:latest


docker logs --since=1h <container_id>
docker logs --since=1h 'container_id' > /path/to/save.txt
cd /var/lib/docker/containers/

docker stop $(docker ps -a -q)
docker rm $(docker ps -a -q)




docker-compose kill
docker rm $(docker ps --filter status=exited -q)
docker volume prune -a -f
docker network prune

docker logs --since=1h 3ff8f80a6376



 
 docker restart $(docker ps -a -q)
 
 docker start $(docker ps -a -q -f status=exited)
 
 
 
 snap install docker          # version 27.2.0, or
 apt  install docker-compose  # version 1.29.2-1
 See 'snap info docker' for additional versions.
 
 
 