# ---------------------------------------------------------------
# ---------------------------------------------------------------
apiVersion: v1
kind: Service
metadata:
  name: tican-gateway-svc
  labels:
    app: tican-gateway-svc
spec:
  type: ClusterIP
  selector:
    app: tican-gateway-pod
  ports:
    - port: 5001
      targetPort: 5001
      
    
  