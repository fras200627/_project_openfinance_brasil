apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tican-discovery-services-pvc
  labels:
    app: tican-discovery-services-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
      
