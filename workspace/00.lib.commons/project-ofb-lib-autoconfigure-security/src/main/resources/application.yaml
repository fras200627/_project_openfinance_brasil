spring:
  application:
    name: ofb-lib-autoconfigure-service

---
spring:
  config:
    activate:
      on-profile: local
---
spring:
  config:
    activate:
      on-profile: docker
---
spring:
  config:
    activate:
      on-profile: k8s
