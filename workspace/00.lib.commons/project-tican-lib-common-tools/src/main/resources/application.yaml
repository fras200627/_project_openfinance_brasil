spring:
  application:
    name: tican-lib-autoconfigure-commons

---
spring:
  config:
    activate:
      on-profile: local

---
spring:
  config:
    activate:
      on-profile: docker

---
spring:
  config:
    activate:
      on-profile: k8s
