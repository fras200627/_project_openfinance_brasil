spring:
  application:
    name: ${AUTHENTICATION_SERVER_APP_NAME:ofb-authentication-server}
  session:
    store-type: jdbc
  security:
    filter:
      dispatcher-types: request
    auth:
      jks:
        keypass: ${AUTH_JKS_KEYPASS:123456}
        storepass: ${AUTH_JKS_STOREPASS:123456}
        alias: ${AUTH_JKS_ALIAS:tican}
        path: ${AUTH_JKS_PATH:keystore/tican.jks}
  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: ${API_JPA_SHOW_SQL:true}
    hibernate:
      ddl-auto: ${API_HIBERNATE_DDL_AUTO:none}

server:
  error:
    include-stacktrace: never

# Custom properties
app:
  listOfServers: #server item: id, description, url
    - 0, 'Tican Server at LOCAL execution', http://localhost/3000
    - 1, 'Tican Server at DOCKER execution', http://localhost/3000
    - 2, 'Tican Server at K8S execution', http://localhost/3000

eureka:
  instance:
    hostname: ${DISCOVERY_SERVICES_APP_NAME:ofb-discovery-services}
    instance-id: ${spring.application.name}:${random.int}
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true

resilience4j:
  circuitBreaker:
    instances:
      authServerDomain:
        waitDurationInOpenState: 50s
        minimumNumberOfCalls: 2
        slidingWindowSize: 3

logging:
  config: classpath:logback/logback-${spring.profiles.active}.xml

# Swagger page: http://localhost:3031/swagger-ui
springdoc:
  api-docs:
    enabled: true
  packagesToScan: com.tican
  pathsToMatch: /**
  swagger-ui:
    enabled: true
    path: /swagger-ui
    disable-swagger-default-url: true
    show-common-extensions: true
    show-extensions: true
    operationsSorter: alpha
    tagsSorter: alpha

---
spring:
  config:
    activate:
      on-profile: local
  security:
    auth:
      provider-uri: http://localhost:3001/ofb-authentication-server
  datasource:
    url: jdbc:mysql://localhost:3306/user?createDatabaseIfNotExist=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${MYSQL_USER:root}
    password: ${MYSQL_ROOT_PWD:root}
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:3000/eureka

# The port can be fixed to "0" so that eureka-server determines a port dynamically
# but in this case access to Swagger-UI will not work
server:
  port: 3031 # set to "0" if you want eureka-server to determine a port dynamically
---
spring:
  config:
    activate:
      on-profile: docker
  security:
    auth:
      provider-uri: http://10.0.1.2:3001/ofb-authentication-server
  datasource:
    url: jdbc:mysql://10.0.1.100:3306/user?createDatabaseIfNotExist=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${MYSQL_USER:root}
    password: ${MYSQL_ROOT_PWD:tican}
eureka:
  client:
    serviceUrl:
      defaultZone: http://10.0.1.1:3000/eureka

# The port can be fixed to "0" so that eureka-server determines a port dynamically
# but in this case access to Swagger-UI will not work
server:
  port: 3031 # set to "0" if you want eureka-server to determine a port dynamically
---
spring:
  config:
    activate:
      on-profile: k8s
  security:
    auth:
      provider-uri: http://10.152.183.107:3001/ofb-authentication-server
  datasource:
    url: jdbc:mysql://10.152.183.209:3306/user?createDatabaseIfNotExist=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${MYSQL_USER:root}
    password: ${MYSQL_ROOT_PWD:root}
eureka:
  client:
    serviceUrl:
      defaultZone: http://10.152.183.103:3000/eureka

# The port can be fixed to "0" so that eureka-server determines a port dynamically
# but in this case access to Swagger-UI will not work
server:
  port: 3031 # set to "0" if you want eureka-server to determine a port dynamically